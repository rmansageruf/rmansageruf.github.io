export const calculateColSpan = _ref => {
  let {
    columns,
    isExpandable,
    isDraggable,
    isSelectable
  } = _ref;
  let totalColSpans = columns.length;
  if (isExpandable) {
    totalColSpans += 1;
  }
  if (isDraggable) {
    totalColSpans += 1;
  }
  if (isSelectable) {
    totalColSpans += 1;
  }
  return totalColSpans;
};