import React from 'react';
import { Cell, WorksheetItem } from '../../types';
export interface CheckboxEditorProps<Item> {
    cell: Cell<Item>;
    toggle: boolean;
    onBlur(): void;
    onChange(value: unknown): void;
}
export declare const CheckboxEditor: <T extends WorksheetItem>({ cell, toggle, onBlur, onChange, }: CheckboxEditorProps<T>) => React.JSX.Element;
//# sourceMappingURL=CheckboxEditor.d.ts.map