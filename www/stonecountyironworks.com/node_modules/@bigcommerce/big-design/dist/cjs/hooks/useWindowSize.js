"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useWindowSize = void 0;
var _utils = require("../utils");
var _useRafState = require("./useRafState");
var _useWindowResizeListener = require("./useWindowResizeListener");
const useWindowSize = () => {
  const [state, setState] = (0, _useRafState.useRafState)({
    height: _utils.isClient ? window.innerHeight : -1,
    width: _utils.isClient ? window.innerWidth : -1
  });
  const resizeHandler = () => {
    setState({
      height: window.innerHeight,
      width: window.innerWidth
    });
  };
  (0, _useWindowResizeListener.useWindowResizeListener)(resizeHandler);
  if (!_utils.isClient) {
    return state;
  }
  return state;
};
exports.useWindowSize = useWindowSize;