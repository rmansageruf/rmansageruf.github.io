"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useAccordionPanel = void 0;
var _react = require("react");
const useAccordionPanel = initialPanels => {
  const [expandedPanels, setExpandedPanels] = (0, _react.useState)(() => {
    return initialPanels.map(_ref => {
      let {
        defaultExpanded
      } = _ref;
      return defaultExpanded ?? false;
    });
  });
  const handleOnClick = panelIndex => () => {
    setExpandedPanels(expandedPanels.map((isExpanded, index) => index === panelIndex ? !isExpanded : isExpanded));
  };
  return {
    panels: expandedPanels.map((isExpanded, index) => ({
      ...initialPanels[index],
      isExpanded,
      onClick: handleOnClick(index)
    }))
  };
};
exports.useAccordionPanel = useAccordionPanel;