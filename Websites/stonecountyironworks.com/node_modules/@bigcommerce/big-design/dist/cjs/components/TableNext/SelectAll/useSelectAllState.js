"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useSelectAllState = void 0;
var _helpers = require("./helpers");
const useSelectAllState = props => {
  const {
    onChange
  } = props;
  const allInPageSelected = (0, _helpers.areAllInPageSelected)(props);
  const someInPageSelected = (0, _helpers.areSomeInPageSelected)(props);
  const totalSelectedItems = props.selectedParentRowsCrossPages.size;
  const label = allInPageSelected ? 'Deselect All' : 'Select All';
  const handleSelectAll = () => {
    if (typeof onChange !== 'function') {
      return;
    }
    const newSelectedItems = (0, _helpers.getSelectAllState)(props);
    return onChange(newSelectedItems);
  };
  return {
    allInPageSelected,
    handleSelectAll,
    label,
    someInPageSelected,
    totalSelectedItems
  };
};
exports.useSelectAllState = useSelectAllState;