import _extends from "@babel/runtime/helpers/esm/extends";
import React, { memo } from 'react';
import { Flex } from '../Flex';
import { Step } from './Step';
export const Stepper = /*#__PURE__*/memo(_ref => {
  let {
    className,
    style,
    steps,
    currentStep = 0,
    ...props
  } = _ref;
  return steps.length ? /*#__PURE__*/React.createElement(Flex, _extends({}, props, {
    "aria-valuemax": steps.length,
    "aria-valuemin": 0,
    "aria-valuenow": currentStep + 1,
    "aria-valuetext": `Step ${currentStep + 1}: ${steps[currentStep]}`,
    flexDirection: "row",
    flexWrap: "nowrap",
    marginBottom: "xLarge",
    role: "progressbar"
  }), steps.map((text, index) => /*#__PURE__*/React.createElement(Step, {
    index: index,
    key: index,
    selectedIndex: currentStep,
    stepperLength: steps.length,
    text: text
  }))) : null;
});
Stepper.displayName = 'Stepper';