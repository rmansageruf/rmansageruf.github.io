import React, { memo, useMemo } from 'react';
import { useWorksheetStore } from '../hooks';
import { Status } from './styled';
export const RowStatus = /*#__PURE__*/memo(_ref => {
  let {
    rowIndex
  } = _ref;
  const {
    store,
    useStore
  } = useWorksheetStore();
  const isSelected = useStore(store, useMemo(() => state => state.selectedRows.includes(rowIndex), [rowIndex]));
  const isInvalid = useStore(store, useMemo(() => state => state.invalidCells.some(invalidCell => invalidCell.rowIndex === rowIndex), [rowIndex]));
  return /*#__PURE__*/React.createElement(Status, {
    isInvalid: isInvalid,
    isSelected: isSelected
  });
});